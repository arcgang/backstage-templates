# ${{ values.name }}

${{ values.description }}

Documentation site published through Backstage TechDocs. Pages live in `docs/`
and the nav is defined in `mkdocs.yml`.

## Preview locally

```sh
pip install mkdocs-techdocs-core
mkdocs serve
```

Then open <http://localhost:8000>.
